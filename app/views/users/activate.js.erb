$("#user_status_<%= @user.id %>").replaceWith('<td id="user_status_<%= @user.id %>"><%= link_to "Disable", disable_account_path(@user),
              method: :patch, data: { confirm: "Are you sure?" }, class: "btn btn-danger", remote: true %></td>')
